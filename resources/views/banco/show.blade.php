@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">banco {{ $banco->codigo_banco }}</div>
                    <div class="card-body">

                        <a href="{{ url('/banco') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/banco/' . $banco->codigo_banco . '/edit') }}" title="Edit banco"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('banco' . '/' . $banco->codigo_banco) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete banco" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $banco->codigo_banco }}</td>
                                    </tr>
                                    <tr><th> Nombre </th><td> {{ $banco->nombre }} </td></tr><tr><th> Codigocuenta </th><td> {{ $banco->codigocuenta }} </td></tr><tr><th> Codigocheque </th><td> {{ $banco->codigocheque }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
