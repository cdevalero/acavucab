@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">actividad {{ $actividad->codigo_actividad }}</div>
                    <div class="card-body">

                        <a href="{{ url('/actividad') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/actividad/' . $actividad->codigo_actividad . '/edit') }}" title="Edit actividad"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('actividad' . '/' . $actividad->codigo_actividad) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete actividad" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $actividad->codigo_actividad }}</td>
                                    </tr>
                                    <tr><th> Nombre </th><td> {{ $actividad->nombre }} </td></tr><tr><th> Tipo </th><td> {{ $actividad->tipo }} </td></tr><tr><th> Fecha Actividad </th><td> {{ $actividad->fecha_actividad }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
