@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">lugar {{ $lugar->codigo_lugar }}</div>
                    <div class="card-body">

                        <a href="{{ url('/lugar') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/lugar/' . $lugar->codigo_lugar . '/edit') }}" title="Edit lugar"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('lugar' . '/' . $lugar->codigo_lugar) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete lugar" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $lugar->codigo_lugar }}</td>
                                    </tr>
                                    <tr><th> Nombre </th><td> {{ $lugar->nombre }} </td></tr><tr><th> Tipo </th><td> {{ $lugar->tipo }} </td></tr><tr><th> Fk Lugar Lugar </th><td> {{ $lugar->fk_lugar_lugar }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
