@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">transferencium {{ $transferencium->codigo_transferencia }}</div>
                    <div class="card-body">

                        <a href="{{ url('/transferencia') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/transferencia/' . $transferencium->codigo_transferencia . '/edit') }}" title="Edit transferencium"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('transferencia' . '/' . $transferencium->codigo_transferencia) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete transferencium" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $transferencium->codigo_transferencia }}</td>
                                    </tr>
                                    <tr><th> Numero Transferencia </th><td> {{ $transferencium->numero_transferencia }} </td></tr><tr><th> Fk Transferencia Banco </th><td> {{ $transferencium->fk_transferencia_banco }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
